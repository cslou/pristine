# API

The public API centers on `createPristineLocal`, memory primitives (`store`, `recall`, `forget`), and privacy primitives (`secureAndRedact`, `reveal`, `scrubOutput`).

Detailed API content will be authored in the public documentation story.
