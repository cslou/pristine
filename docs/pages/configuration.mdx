# Configuration

`Pristine.create()` uses a local SQLite database and local embedder by default. Applications can inject a `better-sqlite3` database or custom embedder; the embedder dimension must match the existing vector index.

Use stable `projectId` values with `store`, `recall`, and `forget` to scope memory correctly. See [Memory](/memory) for project scoping.
