# Examples

The supported reference integration is [Pi-dev](/pi-dev). Its JSONL session files remain the source of truth while Pristine stores a local semantic index and returns source pointers for exact-context inspection.
